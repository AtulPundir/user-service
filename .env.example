# Database Configuration
DATABASE_URL=jdbc:postgresql://localhost:5432/user_db
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres

# Redis Configuration
REDIS_URL=redis://localhost:6379

# JWT Configuration (must match auth-service)
JWT_SECRET=your-super-secret-jwt-key-must-be-at-least-32-characters

# Service Configuration
PORT=3002

# External Service Integration
SUBSCRIPTION_SERVICE_API_KEY=your-subscription-service-api-key

# Optional Configuration
DEFAULT_MONTHLY_LIMIT=50
UNLIMITED_PLAN_LIMIT=-1
CORS_ORIGIN=*

# Spring Profile
SPRING_PROFILES_ACTIVE=development
